[phases.setup]
nixPkgs = ["fontconfig", "freetype", "libpng", "libjpeg", "giflib", "librsvg", "pango", "cairo", "gdk-pixbuf"]

[phases.install]
cmds = [
  "echo 'Usando fuentes embebidas del proyecto - no se requieren fuentes del sistema'"
]

[start]
cmd = "npm start"